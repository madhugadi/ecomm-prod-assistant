SELECT
    COUNT(*) AS TotalRecords,
    SUM(CASE WHEN PERSON_FULL_NAME IS NOT NULL AND LTRIM(RTRIM(PERSON_FULL_NAME)) <> ''
              AND (PERSON_FIRST_NAME IS NULL OR LTRIM(RTRIM(PERSON_FIRST_NAME)) = '')
              AND (PERSON_LAST_NAME IS NULL OR LTRIM(RTRIM(PERSON_LAST_NAME)) = '')
         THEN 1 ELSE 0 END) AS FullNameOnly_FirstLastEmpty
FROM dbo.Bronze_PII_Table_Raw;
