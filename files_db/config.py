SELECT TOP 30
    PERSON_FIRST_NAME,
    PERSON_MIDDLE_NAME,
    PERSON_LAST_NAME,
    PERSON_FULL_NAME,
    PERSON_EMAIL,
    PERSON_TAX_ID,
    PERSON_ADDRESS_FULL
FROM dbo.Bronze_PII_Table_Raw
WHERE LTRIM(RTRIM(PERSON_FIRST_NAME)) IN ('.', '-')
   OR LTRIM(RTRIM(PERSON_MIDDLE_NAME)) IN ('.', '-')
   OR LTRIM(RTRIM(PERSON_LAST_NAME)) IN ('.', '-')
   OR LTRIM(RTRIM(PERSON_FULL_NAME)) IN ('.', '-')
ORDER BY NEWID();
